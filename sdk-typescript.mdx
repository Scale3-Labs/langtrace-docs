---
title: Langtrace Typescript SDK
sidebarTitle: Typescript SDK
---

<Card title="TypeScript SDK Repository" icon="github">
  Check out our TypeScript SDK on GitHub: [Scale3-Labs/langtrace-typescript-sdk](https://github.com/Scale3-Labs/langtrace-typescript-sdk)
</Card>

<Warning>
  **Important Package Name Note**: The TypeScript SDK package name uses `langtrase` with an 's' (NOT `langtrace`).
</Warning>

## Getting Started

<Steps>
  <Step title="Sign Up">
    Create your account at [Langtrace](https://langtrace.ai) and create a new Project to generate your API key.
  </Step>

  <Step title="Installation">
    Install the Langtrace TypeScript SDK using npm:
    ```bash Shell
    # Install the SDK
    npm i @langtrase/typescript-sdk
    ```
  </Step>

  <Step title="Initialize SDK">
    Initialize the SDK with your API key:
    ```typescript
    # Import it into your project. Must precede any llm module imports
    import * as Langtrace from "@langtrase/typescript-sdk";
    Langtrace.init({ api_key: '<LANGTRACE_API_KEY>'})
    ```
  </Step>
</Steps>

## Self-Hosted Setup

<Note>
  For self-hosted Langtrace installations, you'll need to specify your custom API host.
</Note>

<CodeGroup>
  ```typescript TypeScript
  import * as Langtrace from "@langtrase/typescript-sdk";
  Langtrace.init({
    api_key: "<YOUR API KEY>",
    batch: false,
    api_host: "http://localhost:3000/api/trace",
  });
  ```
</CodeGroup>

<Card title="Configuration Note">
  Replace `http://localhost:3000/api/trace` with your own DNS or IP address for self-hosted installations.
</Card>

<Card title="Next Steps" icon="arrow-right">
  For more SDK features and options, check out our [features section](/features).
</Card>
